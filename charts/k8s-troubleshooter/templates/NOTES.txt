k8s-troubleshooter has been installed.

Check pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Port-forward the API:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "k8s-troubleshooter.fullname" . }} 8000:{{ .Values.service.port }}
  curl localhost:8000/health

{{- if .Values.frontend.enabled }}
Port-forward the UI:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "k8s-troubleshooter.fullname" . }}-frontend 3000:80
{{- end }}

LLM provider currently set to: {{ .Values.llm.provider }}
{{- if eq .Values.llm.provider "ollama" }}
Make sure an Ollama service is reachable at {{ .Values.llm.ollama.url }}.
{{- end }}

RBAC: this release created a ClusterRole with read-only access to pods,
deployments, events, nodes, HPAs and PDBs. See SECURITY.md for details.
